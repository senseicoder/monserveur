# Variables requises par le rôle Ansible infra-deploy
# Copier ansible/group_vars/all/vault.yml.example en vault.yml et chiffrer

# Token d'authentification mindwtr-cloud (générer : cat /dev/urandom | LC_ALL=C tr -dc 'a-zA-Z0-9' | fold -w 50 | head -n 1)
MINDWTR_TOKEN=changeme
